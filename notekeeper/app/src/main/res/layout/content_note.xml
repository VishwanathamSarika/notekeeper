<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:showIn="@layout/activity_note"
        tools:context=".NoteActivity" android:layout_margin="10dp" android:padding="5dp">

    <Spinner
            android:layout_width="0dp"
            android:layout_height="wrap_content" android:id="@+id/spinner_courses"
            app:layout_constraintRight_toRightOf="parent" app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" android:layout_marginTop="16dp"
            android:layout_marginRight="16dp" android:layout_marginLeft="16dp"/>
    <EditText
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:inputType="textMultiLine"
            android:ems="10"
            android:id="@+id/text_note_title"
            app:layout_constraintTop_toBottomOf="@+id/spinner_courses"
            app:layout_constraintRight_toRightOf="@+id/spinner_courses"
            app:layout_constraintLeft_toLeftOf="@+id/spinner_courses" android:layout_marginTop="8dp"
            android:hint="Note Title"/>
    <EditText
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:inputType="textMultiLine"
            android:ems="10"
            android:id="@+id/text_note_text"
            app:layout_constraintTop_toBottomOf="@+id/text_note_title"
            app:layout_constraintRight_toRightOf="@+id/text_note_title"
            app:layout_constraintLeft_toLeftOf="@+id/text_note_title" android:layout_marginTop="8dp"
            android:hint="Note Text"/>
</android.support.constraint.ConstraintLayout>